https://www.youtube.com/watch?v=WZcxJGmLbSo

We need to check he vae the dependencies but if we are working in group we can put npm install 
in the folder

Install node js y npm in your computer
https://docs.npmjs.com/downloading-and-installing-node-js-and-npm
install : npm install -g npm (-g = global)
verify version: node -v , npm -v

Install Yarn:
	npm i -g  (i = install)
	yarn install 
	yarn -v 

If cannot install or have errors you need to run in the powershell setExecutionPolicy Unrestricted

Into a folder do you will get the app to install all dependencies there

Do 
 npm init 
if you have a proyect to create a package.json

Then you can get the express package beacause is intalled in package.json 



Install prisma:
	yarn add prisma --dev
	yarn add @prisma/cli --dev
	yarn add @prisma/client
	yarn run prisma init  if does not work try npm install -g prisma
To uninstall yarn remove @prisma/cli
Install express: 
	npm i -s express (-s flag is equals to save in the proyect)

Install ejs
	npm install ejs -save
Install body parser
	npm install body parser --save




to run the page es node index.js

Install api package to this map
	https://www.npmjs.com/package/@react-google-maps/api
------->>>
		with NPM	
			-npm i -S @react-google-maps/api
		with Yarn
			-yarn add @react-google-maps/api

To our node.js proyect front end
npm install -g nodemon (-g install global)
npm install express --save (we are installed yet)
npm install ejs --save
npm install body-parser --save


---------------------------------------------------------------------------------------------

TO THE API GOOGLE MAP


---------------------------------------------------------------------------------------------

Install usePlacesAutocomplete package to get people location google places 
	https://www.npmjs.com/package/use-places-autocomplete

	 	yarn add use-places-autocomplete
 		or
		npm install --save use-places-autocomplete

		typescript too

		yarn add --dev @types/google.maps
		or
		npm install --save-dev @types/google.maps
		then, we need the api permisions and get the key 

		https://developers.google.com/maps/documentation/javascript/places#enable_apis
		https://developers.google.com/maps/documentation/javascript/get-api-key	

		Get api key : https://youtu.be/2_HZObVbe-g
		

Install combobox to show results
	https://reach.tech/combobox/

	npm install @reach/combobox
	# or
	yarn add @reach/combobox


Apps

GeolocationgetCurrentPosition():
	https://developer.mozilla.org/en-US/docs/Web/API/Geolocation/getCurrentPosition

Sleazzymaps:
	https://snazzymaps.com/style/8097/wy
SVGrepo to que images:
	https://www.svgrepo.com/svg/388230/people


OUR KEY: 
AIzaSyBnx_fuHjBGXSitB9XmSq5OcIShkBvjI7M